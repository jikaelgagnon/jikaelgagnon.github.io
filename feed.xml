<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jikaelgagnon.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jikaelgagnon.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-07-09T02:27:41+00:00</updated><id>https://jikaelgagnon.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Stats and probability theory I wished I learned before ML</title><link href="https://jikaelgagnon.github.io/blog/2025/covariance/" rel="alternate" type="text/html" title="Stats and probability theory I wished I learned before ML"/><published>2025-07-07T00:00:00+00:00</published><updated>2025-07-07T00:00:00+00:00</updated><id>https://jikaelgagnon.github.io/blog/2025/covariance</id><content type="html" xml:base="https://jikaelgagnon.github.io/blog/2025/covariance/"><![CDATA[<h1 id="overview">Overview</h1> <p>The goal of this blog is to go over some important concepts in statistics and probability theory that should be well understood to get a stronger grasp on machine learning. This blog post assumes the following prequisite knowledge:</p> <ol> <li>A strong grasp on math notation</li> <li>Basic probability theory (eg. Kolmogorov axioms, CDF, PDF)</li> <li>Basic linear algebra (eg. vectors, matrices, vector spaces, bases, coordinates, and eigenvalues/vectors)</li> <li>Being able to read NumPy code.</li> </ol> <p>I will begin with crucial basics and work up to some other important concepts.This post combines theory and code; you learn the concept using math and see it in action using code.</p> <h1 id="the-basics">The basics</h1> <h2 id="expected-value">Expected value</h2> <p>For a random variable $X$, the <strong>expected value</strong> of $X$, denoted $\mathbb{E}[X]$ is the value you would <strong>expect</strong> if you averaged over an infinite number of trials. The way in which this is computed depends on whether $X$ is a discrete or a continuous random variable.</p> <h3 id="discrete-case">Discrete case</h3> <p>When $X$ is a random variable with a finite list of possible outcomes, $x_1, x_2, \dots, x_N$, then</p> \[\mathbb{E}[X] = \sum_{i=1}^N P(X=x_i) x_i\] <p>This can be seen as a weighted sum of values, where the weight for $x_i$ is $P(X=x_i)$, so values that are more likely will contribute to the sum.</p> <h4 id="example">Example</h4> <p>Let $X = 0$ denote a coinflip of heads, and $X = 1$ denote tails, where $P(X = 0) = P(X = 1) = 1/2$, then our expected value should be directly in the middle of zero and one, since each outcome has the same probability (or weight); indeed, when we plug this into the equation above, we see that</p> \[E[X] = 0*P(X = 0) + 1*P(X = 1) = 0*(1/2) + 1*(1/2) =1/2\] <p>Now, what if instead we had a biased coin? That is, what if one outcome was much more likely than the other? For example, let’s set $P(X = 0) = 0.1$ and $P(X = 1) = 0.9$. In this case, our expected value should be much closer to 1 than 0.9, since tails is a lot more likely. Once again, we see that the the output of the equation matches our intuition:</p> \[E[X] = 0*P(X = 0) + 1*P(X = 1) = 0*(0.1) + 1*(0.9) = 0.9\] <h3 id="continuous-case">Continuous case</h3> <h4 id="the-notion-of-likelihood">The notion of likelihood</h4> <p>In the continuous case, we have an infinite number of possible outcomes, which means that $P(X=x) = 0$ for all possible $x$. To build intuition, consider a proof by contradiction. Suppose there exists some nonzero probability $\epsilon_x &gt; 0$ assigned to each individual outcome $x$. Then, summing over an infinite number of such outcomes would yield an infinite total probability:</p> \[\sum_{x \in \mathbb{R}} P(X = x) = \sum_{x \in \mathbb{R}} \epsilon_x = \infty\] <p>which contradicts the requirement that the total probability over the sample space must equal 1. Therefore, the only consistent assignment is that each individual outcome has probability 0.</p> <p>You might be thinking “Ok, I understand why probabilities are zero, but surely there are some events that are more likely than others”. This is indeed true. For example, in theory, height is a continuous random variable; there are certainly some heights that are more common than others. This idea is captured through the notion of <strong>likelihood</strong>. For each possible value of $x$, we can assign a value $p(x) \geq 0$ which represents how likely we are to observe $x$ (it is very important to keep in mind that it’s not actually the <em>probability</em> of $x$). We add the additional restriction that $\int_{-\infty}^{+\infty}p(x)dx = 1$ (so the area under the likelihood curve is 1).</p> <h4 id="coming-up-with-an-equation-for-expected-value">Coming up with an equation for expected value</h4> <p>Recall that earlier our expected value was computed as a weighted sum of values, where the weights are point probabilities. In the continuous case, we have seen that probabilities are swapped out for likelihoods, and since we have an infinite number of values, we should take an integral. Thus, our expected value can be written as</p> \[E[X] = \int_{-\infty}^{+\infty}p(x)xdx\] <p>Again, the idea here is that the points with higher likelihood have a greater weight in the computing the expected value.</p> <h4 id="example-1">Example</h4> <blockquote class="block-warning"> <h5 id="important-note">Important Note</h5> <p>A concept that is often glossed over is that in practice we typically don’t compute the actual expected value, since that would require knowing the true underlying distribution of our data (which is often unrealistic). Instead, we estimate it using an <strong>unbiased estimator</strong>. This is a value that we can compute using our dataset that <em>estimates</em> the true value of some parameter; as the amount of data increases, we approach the true value. More formally, an estimator $\hat{\theta}$ is said to be <em>unbiased</em> for parameter $\theta$ if $\mathbb{E}[\hat{\theta}] = \theta$. For example, suppose we are trying to estimate $\mu_X = \mathbb{E}[X]$; it can be shown that the sample mean $\bar{X} = \frac{1}{N} \sum_{i=1}^N x^{(i)}$ is an unbiased estimator of $\mu_X$ (so $\mathbb{E}[\bar{X}] = \mu_X$). As we will see later, unbiased estimators also exist for many other parameters (such as the variance and covariance).</p> </blockquote> <p>For the continuous case, showing a worked out example is a bit trickier. Instead, we will take some real world data and <em>estimate</em> the expected value using an unbiased estimator. An unbiased estimator for the expected value is the sample mean (typically denoted $\bar{X}$):</p> \[\bar{X} = \frac{1}{N} \sum_{i = 1}^N x^{(i)}\] <p>Again, remember that this is not actually the expected value. This is a value we can compute to estimate the expected value.</p> <p>For our real world data, I downloaded <a href="https://www.kaggle.com/datasets/mustafaali96/weight-height">height and weight data from Kaggle</a>. For now, we will focus on only the height data.</p> <p>First, it would be nice to plot the probabilities of each height; the problem is that heights are a continous variable, so the probability of an exact height is very low (eg. the odds that someone has a height of 66.72 inches is quite low, but the probability of a height <em>close to</em> 66.72 might be quite high). To deal with this, we will group our data points into <em>bins</em>. These are simply intervals in the data; thus, instead of counting the probability of an exact value, we will compute the probability that a point falls into a bin. Here is a visualization of our data being split into $n=30$ bins, where the boundaries of the bins are marked by red-dotted lines:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/bins-480.webp 480w,/assets/img/bins-800.webp 800w,/assets/img/bins-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/bins.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Using the same bins, we can plot the density of our data (where density for a bin is ( of points in bin) / (total of points)):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">import</span> <span class="n">matplotlib.pyplot</span> <span class="k">as</span> <span class="n">plt</span>


<span class="n">df</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">./heights/SOCR-HeightWeight.csv</span><span class="sh">"</span><span class="p">)</span>
<span class="n">heights</span> <span class="o">=</span> <span class="n">df</span><span class="p">[</span><span class="sh">"</span><span class="s">Height(Inches)</span><span class="sh">"</span><span class="p">].</span><span class="nf">to_numpy</span><span class="p">()</span>
<span class="n">n_bins</span> <span class="o">=</span> <span class="mi">30</span>
<span class="n">bin_centers</span><span class="p">,</span> <span class="n">probabilities</span> <span class="o">=</span> <span class="nf">split_bins_and_probabilities</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">n_bins</span><span class="p">)</span>

 <span class="n">Plot</span> <span class="n">the</span> <span class="n">histogram</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">hist</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">bins</span><span class="o">=</span><span class="mi">30</span><span class="p">,</span> <span class="n">density</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mf">0.5</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">xlabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Height</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">ylabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Density</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">title</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Height densities with n=</span><span class="si">{</span><span class="n">n_bins</span><span class="si">}</span><span class="s"> bins</span><span class="sh">"</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/height_density-480.webp 480w,/assets/img/height_density-800.webp 800w,/assets/img/height_density-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/height_density.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Now that the concept of bins is (hopefully) clear, the plot should hopefully make sense. Now we can simply compute the expected value. As seen above, we can simply do this by calculating the mean</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">E</span><span class="p">(</span><span class="n">X</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>

<span class="nc">E</span><span class="p">(</span><span class="n">heights</span><span class="p">)</span>  <span class="n">returns</span> <span class="mf">67.99</span>
</code></pre></div></div> <p>Using this, let’s plot our data again, this time including the mean:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">E_height</span> <span class="o">=</span> <span class="nc">E</span><span class="p">(</span><span class="n">heights</span><span class="p">)</span>

<span class="n">plt</span><span class="p">.</span><span class="nf">hist</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">density</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">bins</span><span class="o">=</span><span class="n">n_bins</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mf">0.5</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">axvline</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="n">E_height</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">'</span><span class="s">red</span><span class="sh">'</span><span class="p">,</span> <span class="n">linestyle</span><span class="o">=</span><span class="sh">'</span><span class="s">--</span><span class="sh">'</span><span class="p">,</span> <span class="n">linewidth</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sa">f</span><span class="sh">'</span><span class="s">Expected value: </span><span class="si">{</span><span class="n">E_height</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">'</span><span class="p">)</span>

<span class="n">plt</span><span class="p">.</span><span class="nf">legend</span><span class="p">()</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">xlabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Height</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">ylabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Density</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">title</span><span class="p">(</span><span class="sh">'</span><span class="s">Distribution of Heights</span><span class="sh">'</span><span class="p">)</span>

<span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/heights_with_mean-480.webp 480w,/assets/img/heights_with_mean-800.webp 800w,/assets/img/heights_with_mean-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/heights_with_mean.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>In this case, our expected value is roughly $68$, which seems to be roughly at the peak of our data. Importantly, note that the expected value will not <em>always</em> be at the peak of the data; the reason it’s in at the peak in this case is because the data is symmetrical around the peak.</p> <h3 id="expected-value-of-the-function-of-a-random-variable">Expected Value of the Function of a Random Variable</h3> <p>When reading machine learning material, you will see expected value come up <em>a lot</em>, but in many cases the notation will look much more complex; for example, $\mathbb{E}[\mathbb{H}(X)] = \int_{\infty}^{\infty} p(x)\mathbb{H}(x)dx$. Do not let this scare you. This is the expected value of a function of a random variable. The concepts here are identical to before: we want to know the value if we average over an infinite number of trials. The only difference here is that before we were looking at the expected value of $X$, and now we’re looking at the expected value of some function of $X$. So to find the average of $\mathbb{H}(x)$ over an infinite number of trials, we multiply $\mathbb{H}(x)$ by the likelihood of the input, $p(x)$.</p> <h4 id="example-2">Example</h4> <p>Let’s dig a bit deeper by looking at the example from above. We will take the height of each person and double it. That is, we will apply the function $f(x) = 2x$ to each height in our dataset, and we will compute the new expected value. Before doing this, can you guess what it will be?</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
    <span class="k">return</span> <span class="mi">2</span><span class="o">*</span><span class="n">x</span>

<span class="n">E_f</span> <span class="o">=</span> <span class="nc">E</span><span class="p">(</span><span class="nf">f</span><span class="p">(</span><span class="n">heights</span><span class="p">))</span>

<span class="n">plt</span><span class="p">.</span><span class="nf">hist</span><span class="p">(</span><span class="nf">f</span><span class="p">(</span><span class="n">heights</span><span class="p">),</span> <span class="n">density</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">bins</span><span class="o">=</span><span class="mi">30</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mf">0.5</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">axvline</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="n">E_f</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">'</span><span class="s">red</span><span class="sh">'</span><span class="p">,</span> <span class="n">linestyle</span><span class="o">=</span><span class="sh">'</span><span class="s">--</span><span class="sh">'</span><span class="p">,</span> <span class="n">linewidth</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sa">f</span><span class="sh">'</span><span class="s">Expected value: </span><span class="si">{</span><span class="n">E_f</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">'</span><span class="p">)</span>

<span class="n">plt</span><span class="p">.</span><span class="nf">legend</span><span class="p">()</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">xlabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Double Height</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">ylabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Density</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">title</span><span class="p">(</span><span class="sh">'</span><span class="s">Distribution of f(x) = 2x</span><span class="sh">'</span><span class="p">)</span>

<span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>

</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/scaled_heights-480.webp 480w,/assets/img/scaled_heights-800.webp 800w,/assets/img/scaled_heights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/scaled_heights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The new expected value $\mathbb{E}[f(X)] = \mathbb{E}[2X]$ is twice the mean from before! This makes sense, since each input of $f$ maps to a unique output, the distribution should remain the same, but with each value doubled (ie. if $x_i$ has likelihood $p_i$ in our original data, then $f(x_i)$ will have the same likelihood in the our new data).</p> <h3 id="key-takeaways-on-expected-value">Key Takeaways on Expected Value</h3> <ol> <li>Expected value tells you the value you would <em>expect</em> is you averaged over the outcome of an infinite number of trials. In other words, it’s a theoretical <em>mean</em> of the data (as such, the terms mean and expected value are often interchangeable).</li> <li>You can take the expected value of functions of random variables.</li> <li>You can estimate the expected value using the sample mean $\bar{X}$.</li> </ol> <h2 id="variance">Variance</h2> <p>For a variable random variable $X$, the <em>variance</em> of $X$ is $\text{Var}(X) = \sigma^2_X = \mathbb{E}[(x - \mathbb{E}[X])^2)]$. This can be read as the average squared difference from the mean. Note this is <em>squared</em>, meaning the variance must be positive. Intuitively, this measures the spread of the data, or how much it <em>varies</em> from the mean. To make this slightly more readable, we will denote the expected value of $X$ as $\mathbb{E}[X] = \mu_X$; this is common notation. Notice that the variance is the expected value of function of a random variable. In this case, it’s the function $f(X) = (X - \mu_X)^2$ (where we used our new notation for the mean). Thus, all the same ideas from above still apply; namely, that the way in which this is computed will depend on whether $X$ is a dicrete or a continuous random variable.</p> <p>In the discrete case:</p> \[\sigma^2_X = \mathbb{E}[(X - \mu_X)^2] = \sum_{i=1}^N P(X=x_i) (x_i - \mu_X)^2\] <p>In the continuous case:</p> \[\sigma^2_X = E[(X - \mu_X)^2] = \int_{-\infty}^{+\infty}p(x)(x - \mu_X)^2dx\] <h3 id="example-3">Example</h3> <p>Let’s look at this in action by jumping back into our example of height data. Again, we will be using an unbiased estimator for the variance. In this case, the unbiased estimator for the variance is the sample variance, often denoted $S^2$:</p> \[S^2 = \frac{1}{N-1} \sum_{i = 1}^N(x^{(i)} - \bar{X})\] <p>note that the reason we divide by $n-1$ is not immediately obvious and is beyond the scope of this blog.</p> <p>We can now start our investigation by writing a function that computes the sample variance. Again, this is pretty straightforward.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">V</span><span class="p">(</span><span class="n">X</span><span class="p">):</span>
    <span class="n">mu</span> <span class="o">=</span> <span class="nc">E</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
    <span class="n">squared_diffs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">((</span><span class="n">X</span> <span class="o">-</span> <span class="nc">E</span><span class="p">(</span><span class="n">X</span><span class="p">))</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span>
    <span class="n">s_2</span> <span class="o">=</span> <span class="n">squared_diffs</span> <span class="o">/</span> <span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">X</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">s_2</span>

<span class="nc">V</span><span class="p">(</span><span class="n">heights</span><span class="p">)</span>  <span class="mf">3.62</span>
</code></pre></div></div> <p>Again, it’s important to note that variance gives us the average <em>squared</em> difference from the mean. To get the non-squared difference you can take the square root of the variance, $\sqrt{Var(X)} = \sqrt{\sigma_X^2} = \sigma_X$; this value is so frequently used that it has a name: the <em>standard deviation</em>. Let’s visualize our data with the variance to see the regions that are one standard deviation to the left and right of the mean:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">Compute</span> <span class="n">expected</span> <span class="n">value</span> <span class="ow">and</span> <span class="n">variance</span> <span class="k">from</span> <span class="n">the</span> <span class="n">raw</span> <span class="n">data</span>
<span class="n">E_height</span> <span class="o">=</span> <span class="nc">E</span><span class="p">(</span><span class="n">heights</span><span class="p">)</span>
<span class="n">var_height</span> <span class="o">=</span> <span class="nc">V</span><span class="p">(</span><span class="n">heights</span><span class="p">)</span>
<span class="n">std_height</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">var_height</span><span class="p">)</span>
 <span class="n">Plot</span> <span class="n">histogram</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">hist</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">density</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">bins</span><span class="o">=</span><span class="n">n_bins</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mf">0.5</span><span class="p">)</span>
 <span class="n">Expected</span> <span class="n">value</span> <span class="n">line</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">axvline</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="n">E_height</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">'</span><span class="s">red</span><span class="sh">'</span><span class="p">,</span> <span class="n">linestyle</span><span class="o">=</span><span class="sh">'</span><span class="s">--</span><span class="sh">'</span><span class="p">,</span> <span class="n">linewidth</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sa">f</span><span class="sh">'</span><span class="s">Expected value: </span><span class="si">{</span><span class="n">E_height</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">'</span><span class="p">)</span>
 <span class="n">Variance</span> <span class="nb">range</span> <span class="nf">lines </span><span class="p">(</span><span class="err">±</span><span class="mi">1</span> <span class="n">std</span> <span class="n">dev</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">axvline</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="n">E_height</span> <span class="o">-</span> <span class="n">std_height</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">'</span><span class="s">blue</span><span class="sh">'</span><span class="p">,</span> <span class="n">linestyle</span><span class="o">=</span><span class="sh">'</span><span class="s">:</span><span class="sh">'</span><span class="p">,</span> <span class="n">linewidth</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sa">f</span><span class="sh">'</span><span class="s">-1σ: </span><span class="si">{</span><span class="n">E_height</span> <span class="o">-</span> <span class="n">std_height</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">axvline</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="n">E_height</span> <span class="o">+</span> <span class="n">std_height</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">'</span><span class="s">blue</span><span class="sh">'</span><span class="p">,</span> <span class="n">linestyle</span><span class="o">=</span><span class="sh">'</span><span class="s">:</span><span class="sh">'</span><span class="p">,</span> <span class="n">linewidth</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sa">f</span><span class="sh">'</span><span class="s">+1σ: </span><span class="si">{</span><span class="n">E_height</span> <span class="o">+</span> <span class="n">std_height</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">'</span><span class="p">)</span>
 <span class="n">Labels</span> <span class="ow">and</span> <span class="n">legend</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">legend</span><span class="p">()</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">xlabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Height</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">ylabel</span><span class="p">(</span><span class="sh">'</span><span class="s">Density</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">title</span><span class="p">(</span><span class="sh">'</span><span class="s">Distribution of Heights with Expected Value and Variance</span><span class="sh">'</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>

</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/height_with_variance-480.webp 480w,/assets/img/height_with_variance-800.webp 800w,/assets/img/height_with_variance-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/height_with_variance.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>If our data were more closely centered around the mean, our variance would be smaller, and if it were more spread out, the variance would be larger.</p> <h3 id="key-takeaways-on-variance">Key Takeaways on Variance</h3> <ol> <li>Variance tells you the expected squared distance from the mean.</li> <li>Variance is the expectation of a function of a random variable.</li> <li>Variance can be estimated using sample variance $S^2$.</li> </ol> <h2 id="higher-dimensional-data">Higher dimensional data</h2> <h3 id="expected-value-in-higher-dimensions">Expected value in higher dimensions</h3> <p>In machine learning, we often deal with higher dimensional data. For example, our input data might contain the heights and weights of various individuals. In this case, each of our data point can be represented as 2D vector, where the first element is the height and the second is the weight. That is, our dataset is a set of points $x^{(i)} = [x^{(i)}<em>1, x^{(i)}_2]$, where $x^{(i)}_1$ is the height of invidual $i$ and $x^{(i)}_2$ is their weight. In this case, instead of computing a scalar for the expected value, we compute a vector $\vec{mu_X} = [\mu</em>{x_1}, \mu_{x_2}]$, where $\mu_{x_1}$ is the expected value looking only at height, and $\mu_{x_2}$ is the mean looking only at weight.</p> <h3 id="vectorizing-our-equations">Vectorizing our equations</h3> <p>In machine learning, we often deal with high-dimensional data (ie. our data points are usually high-dimensional vectors). In machine learning, we will often vectorize equations to create concise versions of equations that scale to any dimensionality (also, when programming, vectorized equations allow you to avoid loops, often making code faster and easier to read). Accordingly, <em>you should get comfortable with vector notation</em>. In the remainder of this blog I will first introduce equations in their “scalar form” and then re-write in their vector notation. It might be helpful to first look at scalar equations, try to figure out the vectorized form, then look to see if you were right.</p> <p>Let’s start this off with the simplest example: the sample mean, $\bar{X}$.</p> <p>Recall the equation for the sample mean:</p> \[\bar{X} = \frac{1}{N} \sum_{i = 1}^N x^{(i)}\] <p>How can we write this in vector form? If our data is a set on $n$ 1D datapoints, $X \in \mathbb{R}^{N \times 1}$, we can compute the sample mean mean as dot product:</p> \[\bar{X} = \frac{1}{N}\mathbb{1}^TX = \frac{1}{N}(1 \cdot x^{(1)} + \dots + 1 \cdot x^{(N)}) = \frac{1}{N}\sum_{i=1}^{N} x^{(i)}\] <p>where $\mathbb{1} \in \mathbb{R}^{n \times 1}$ is a vector of ones. Now what if we want to generalize this even further? That is, what if our data points aren’t just 1D, but $d$-dimensional? Suppose we have a dataset $X \in \mathbb{R}^{n \times d}$. It turns out we can do the exact same thing! The result is now a $1 \times d$ vector where</p> \[\bar{X}_j = \frac{1}{N} \sum_{i = 1}^{N} x_j^{(i)}\] <p>In words, the $j$-th component is the mean of the $j$-th feature. In other words, we have computed the mean of each column of our dataset.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">E</span><span class="p">(</span><span class="n">X</span><span class="p">):</span>
    <span class="n">N</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">/</span> <span class="n">N</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">N</span><span class="p">).</span><span class="n">T</span> <span class="o">@</span> <span class="n">X</span>
    <span class="k">return</span> <span class="n">mean</span>
</code></pre></div></div> <h4 id="example-4">Example</h4> <p>Again, let’s understand this via an example. This time, we will need 2D data. Luckily, the data from earlier contains both the heights and weights of individuals. To start, we will simply plot our data to visualize it.</p> <p>We can easily compute our expected value vector using the equation above:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">Get</span> <span class="n">the</span> <span class="n">data</span>
<span class="n">df</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">./heights/SOCR-HeightWeight.csv</span><span class="sh">"</span><span class="p">)</span>
<span class="n">heights</span> <span class="o">=</span> <span class="n">df</span><span class="p">[</span><span class="sh">"</span><span class="s">Height(Inches)</span><span class="sh">"</span><span class="p">].</span><span class="nf">to_numpy</span><span class="p">()</span>
<span class="n">weights</span> <span class="o">=</span> <span class="n">df</span><span class="p">[</span><span class="sh">"</span><span class="s">Weight(Pounds)</span><span class="sh">"</span><span class="p">].</span><span class="nf">to_numpy</span><span class="p">()</span>
<span class="n">X</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="n">heights</span><span class="p">,</span> <span class="n">weights</span><span class="p">]).</span><span class="n">T</span>

<span class="n">mu_height</span><span class="p">,</span> <span class="n">mu_weight</span> <span class="o">=</span> <span class="nc">E</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>

 <span class="n">Plot</span> <span class="n">raw</span> <span class="n">data</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">scatter</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">weights</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mf">0.2</span><span class="p">,</span> <span class="n">s</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">"</span><span class="s">blue</span><span class="sh">"</span><span class="p">,</span> <span class="n">marker</span><span class="o">=</span><span class="sh">"</span><span class="s">x</span><span class="sh">"</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sh">'</span><span class="s">Raw data</span><span class="sh">'</span><span class="p">)</span>

 <span class="n">Plot</span> <span class="n">mean</span> <span class="n">point</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">scatter</span><span class="p">(</span><span class="n">mu_height</span><span class="p">,</span> <span class="n">mu_weight</span><span class="p">,</span> <span class="n">color</span><span class="o">=</span><span class="sh">"</span><span class="s">red</span><span class="sh">"</span><span class="p">,</span> <span class="n">s</span><span class="o">=</span><span class="mi">100</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="sh">'</span><span class="s">Mean vector (μ)</span><span class="sh">'</span><span class="p">,</span> <span class="n">marker</span><span class="o">=</span><span class="sh">'</span><span class="s">o</span><span class="sh">'</span><span class="p">)</span>

 <span class="n">Annotate</span> <span class="nf">mean </span><span class="p">(</span><span class="n">optional</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">annotate</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">μ = (</span><span class="si">{</span><span class="n">mu_height</span><span class="si">:</span><span class="p">.</span><span class="mi">1</span><span class="n">f</span><span class="si">}</span><span class="s">, </span><span class="si">{</span><span class="n">mu_weight</span><span class="si">:</span><span class="p">.</span><span class="mi">1</span><span class="n">f</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">,</span> 
             <span class="p">(</span><span class="n">mu_height</span><span class="p">,</span> <span class="n">mu_weight</span><span class="p">),</span>
             <span class="n">textcoords</span><span class="o">=</span><span class="sh">"</span><span class="s">offset points</span><span class="sh">"</span><span class="p">,</span>
             <span class="n">xytext</span><span class="o">=</span><span class="p">(</span><span class="mi">10</span><span class="p">,</span><span class="mi">10</span><span class="p">),</span> 
             <span class="n">ha</span><span class="o">=</span><span class="sh">'</span><span class="s">left</span><span class="sh">'</span><span class="p">,</span>
             <span class="n">fontsize</span><span class="o">=</span><span class="mi">12</span><span class="p">,</span>
             <span class="n">color</span><span class="o">=</span><span class="sh">"</span><span class="s">red</span><span class="sh">"</span><span class="p">)</span>

 <span class="n">Labels</span> <span class="ow">and</span> <span class="n">legend</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">xlabel</span><span class="p">(</span><span class="sh">"</span><span class="s">Height (Inches)</span><span class="sh">"</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">ylabel</span><span class="p">(</span><span class="sh">"</span><span class="s">Weight (Pounds)</span><span class="sh">"</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">legend</span><span class="p">()</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">title</span><span class="p">(</span><span class="sh">"</span><span class="s">Height vs. Weight with Mean Vector</span><span class="sh">"</span><span class="p">)</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/height_weight-480.webp 480w,/assets/img/height_weight-800.webp 800w,/assets/img/height_weight-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/height_weight.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>As we saw above, the mean can be generalized to $d$-dimensions. The subsequent section will go over covariance, which is the higherdimensional generalization of variance.</p> <h3 id="key-takeaways-on-higher-dimensional-data">Key Takeaways on Higher Dimensional Data</h3> <ol> <li>Machine learning uses a lot of high dimensional data</li> <li>We can treat each feature as its own random variable</li> <li>It’s important to understand vecorized equations</li> </ol> <h2 id="covariance">Covariance</h2> <p>The <em>covariance</em> between two random variables $X$ and $Y$ is $\text{Cov}(X,Y) = \mathbb{E}[(X - \mu_X)(Y - \mu_Y)]$. This equation takes every possible pair of $X$ and $Y$ and takes the product of their differences from their respective means. Intuitively, if this is:</p> <ol> <li>Positive it means that on average, when $X$ is greater than its mean, so is $Y$ (and vice-versa), which means they grow in the same direction.</li> <li>Negative means that on average, when $X$ is above its mean then $Y$ is below (or vice-versa), which means they grow in opposite directions.</li> <li>Close to 0 means neither of the above are true, so it must mean there is no relationship in the direction in which they grow (not the same nor the opposite)</li> </ol> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/covariance-480.webp 480w,/assets/img/covariance-800.webp 800w,/assets/img/covariance-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/covariance.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>As usual, the way in which this is computed depends on whether we’re in the discrete or continuous case:</p> <p>In the discrete case:</p> \[\text{Cov}(X,Y) = \mathbb{E}[(X - \mu_X)(Y - \mu_Y)] = \sum_{i=1}^N \sum_{j=1}^M P(X=x_i, Y=y_j) (x_i - \mu_X)(y_j - \mu_Y)\] <p>In the continuous case:</p> \[\text{Cov}(X,Y) = \mathbb{E}[(X - \mu_X)(Y - \mu_Y)] = \int_{-\infty}^{+\infty}\int_{-\infty}^{+\infty}p(x,y)(x - \mu_X)(y - \mu_Y)dxdy\] <p>Note that $\text{Cov}(X,X) = \text{Var}(X)$.</p> <p>When working with higher dimensional data, we are interested in the covariance between all combinations of random variables. This introduces the notion of the <em>covariance matrix</em>, $\Sigma$. Given random variables $X_1, \dots, X_d$ we define $\Sigma \in \mathbb{R}^{d \times d}$ such that</p> \[\Sigma_{i,j} = \text{Cov}(X_i, X_j)\] <p>This definition implies that the elements on the main diagonal are of the form $\Sigma_{i,i} = \text{Cov}(X_i, X_i) = \text{Var}(X_i)$. We will do a much deeper dive into the covariance matrix later.</p> <h3 id="unbiased-estimator-and-vectorized-version">Unbiased estimator and vectorized version</h3> <p>As usual, we will not actually compute the covariance / covariance matiex. Instead, we will use an unbiased estimator. This time it’s the estimator $S_{XY}$:</p> \[S_{XY} = \frac{1}{N-1} \sum_{i=1}^{N}(X - \bar{X})(Y - \bar{Y})\] <p>Again, let’s start with the simplest case and work our way up. So, we will start by seeing how we can vectorize variance (which is just a special case of covariance). If our data is a set of $n$ 1D datapoints, $X \in \mathbb{R}^{N \times 1}$, we can compute the sample variance as a dot product:</p> \[S^2 = \frac{1}{N - 1} (X - \bar{X})^T(X - \bar{X})^T = \frac{1}{N - 1} [(x^{(1)} - \bar{X}) \cdot (x^{(1)} - \bar{X}) + \dots + (x^{(N)} - \bar{X}) \cdot (x^{(N)} - \bar{X})] = \frac{1}{N-1} \sum_{i=1}^{N}(x^{(i)} - \bar{X})^2\] <p>Again, it turns out that this exact same formulation generalizes to $d$-dimensional data, where</p> \[S^2_{k,j} = \frac{1}{N-1} \sum_{i=1}^{N}(x^{(i)}_k - \bar{X}_k)(x^{(i)}_j - \bar{X}_j)\] <p>Using code, we can compute the covariance matrix:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Cov</span><span class="p">(</span><span class="n">X</span><span class="p">):</span>
    <span class="n">n</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="nc">E</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
    <span class="n">diff</span> <span class="o">=</span> <span class="p">(</span><span class="n">X</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span>
    <span class="n">cov</span> <span class="o">=</span> <span class="mi">1</span><span class="o">/</span><span class="p">(</span><span class="n">n</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">diff</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">diff</span>
    <span class="k">return</span> <span class="n">cov</span>
</code></pre></div></div> <h3 id="example-5">Example</h3> <p>Let’s look at each of the values in the covariance matrix for our height and weight data:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Cov</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">weights</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>array([[  3.61638215,  11.15102917],
       [ 11.15102917, 135.97653199]])
</code></pre></div></div> <p>The first element on the diagonal is the variance of the height (which is the exact same value we computed before). The second is the variance of the weight. The values on the outer diagonal are the same, and are the covariance of the height and weight. Notice that it’s positive, which is what we would expect; a positive covariance means that both height and weight grow together (ie. as weight increases height also increases).</p> <h3 id="key-takeaways-on-covariance">Key Takeaways on Covariance</h3> <ol> <li>Covariance tells you how two random variables grow together</li> <li>Variance is a special case of covariance</li> <li>The covariances for all combinations of random variables in the dataset are represented in the covariance matrix</li> <li>Covariance can be estimated using $S_{XY}$</li> </ol> <h2 id="correlation">Correlation</h2> <p>Often when we’re looking at the relationship between two variables we care more about the correlation than the covariance. The idea behind correlation is to <em>normalize</em> our covariance; this is a common term in machine learning. Generally, it means to rescale your data to fit in a particular range. For example, a vector can be normalized into a unit vector, which rescales it to a vector of length one. In this context, we normalize our covariance into the range $[-1,1]$ to obtain our correlation. Correlation is defined as:</p> \[\rho_{X,Y} = \text{corr}(X,Y) = \frac{\text{cov}(X,Y)}{\sigma_X \sigma_Y} = \frac{\mathbb{E}(X - \mu_X)(Y - \mu_Y)}{\sigma_X \sigma_Y} \in [-1,1]\] <p>where the denominator normalizes the data. The reason why this normalizes the data might not immediately be obvious. To see this, let’s look at the vector form of the unbiased estimator seen above (here $X, Y\in \mathbb{R}^n$ so both are simply vectors):</p> \[\rho_{X,Y} = \frac{\text{cov}(X,Y)}{\sigma_X \sigma_Y} = \frac{(X - \bar{X})^T (Y - \bar{Y})}{\sqrt{(X - \bar{X})^T(X - \bar{X})} \cdot \sqrt{(Y - \bar{Y})^T(Y - \bar{Y})}} = \left( \frac{(X - \bar{X})}{\|X - \bar{X}\|} \right)^T \left( \frac{(Y - \bar{Y})}{\|Y - \bar{Y}\|} \right)\] <p>Thus, this can be seen as the dot product of two unit vectors. This is exactly the <strong>cosine of the angle</strong> $\theta$ between the two centered data vectors Since $\cos(\theta) \in [-1, 1]$, the correlation is always in that range.</p> <h3 id="why-is-this-useful">Why is this useful?</h3> <p>Unlike covariance which can be any real number, correlation gives a standardized way to measure how related two variables are. As such, it gives us a way to compare results across different experiments with data of different scales.</p> <h3 id="example-6">Example</h3> <p>Using our dataset, we can compute the correlation of our data:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">corr</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">Y</span><span class="p">):</span>
    <span class="n">centered_x</span> <span class="o">=</span> <span class="n">X</span> <span class="o">-</span> <span class="nc">E</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
    <span class="n">centered_x_norm</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">centered_x</span><span class="p">)</span>
    
    <span class="n">centered_y</span> <span class="o">=</span> <span class="n">Y</span> <span class="o">-</span> <span class="nc">E</span><span class="p">(</span><span class="n">Y</span><span class="p">)</span>
    <span class="n">centered_y_norm</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">centered_y</span><span class="p">)</span>
    
    <span class="n">corr</span> <span class="o">=</span> <span class="p">(</span><span class="n">centered_x</span><span class="p">).</span><span class="n">T</span> <span class="o">@</span> <span class="n">centered_y</span> <span class="o">/</span> <span class="p">(</span><span class="n">centered_x_norm</span> <span class="o">*</span> <span class="n">centered_y_norm</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">corr</span>

<span class="nf">corr</span><span class="p">(</span><span class="n">heights</span><span class="p">,</span> <span class="n">weights</span><span class="p">)</span>  <span class="mf">0.50</span>
</code></pre></div></div> <p>We get a correlation of 0.50, which means height are weight are moderately correlated.</p> <h3 id="key-takeaways-on-correlation">Key Takeaways on Correlation</h3> <ol> <li>Correlation is just normalized covariance</li> <li>Correlation provides a way to compare data from different experiments</li> </ol> <h1 id="a-deep-dive-into-the-covariance-matrix">A deep dive into the covariance matrix</h1> <p>TODO…</p> <h1 id="the-gaussian">The Gaussian</h1> <p>TODO…</p>]]></content><author><name></name></author><category term="ml,"/><category term="stats,"/><category term="learning"/><summary type="html"><![CDATA[Covering important aspects of statistics and probability that are frequently glossed over in ML teaching.]]></summary></entry></feed>